import {NextResponse} from 'next/server';import {db} from '@/lib/db';import {getCurrentUser} from '@/lib/auth';export async function POST(req:Request){const u=await getCurrentUser();if(!u)return NextResponse.json({error:'Giriş gerekli'},{status:401});const subscription=await req.json();await (await db()).collection('push_subscriptions').updateOne({userId:u._id,'subscription.endpoint':subscription.endpoint},{$set:{userId:u._id,subscription,updatedAt:new Date()}},{upsert:true});return NextResponse.json({ok:true})}
