'use client';import {useState} from 'react';export default function PushSetup(){const [state,setState]=useState('');async function enable(){try{if(!('serviceWorker'in navigator)||!('PushManager'in window)){setState('Bu cihaz push bildirimini desteklemiyor.');return}const permission=await Notification.requestPermission();if(permission!=='granted'){setState('Bildirim izni verilmedi.');return}const reg=await navigator.serviceWorker.register('/sw.js');const key=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY;if(!key){setState('VAPID public key yapılandırılmamış.');return}const sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:Uint8Array.from(atob(key.replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0))});const x=await fetch('/api/push/subscribe',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(sub)});setState(x.ok?'Bu cihaz bildirim için kayıtlı.':'Kayıt başarısız.')}catch(e){setState('Bildirim kurulumu başarısız.')}}return <div className="card" style={{marginTop:16}}><div className="row" style={{justifyContent:'space-between'}}><div><b>Bildirim cihazı</b><div className="muted">Web Push + uygulama içi bildirim</div></div><button className="btn" onClick={enable}>Bildirimleri Etkinleştir</button></div>{state&&<p className="muted">{state}</p>}</div>}
