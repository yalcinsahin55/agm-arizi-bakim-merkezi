export default function BreakdownSummary({ breakdown }: {
    breakdown: import('@/types').Breakdown;
}) { const duration = breakdown.startedAt && breakdown.closedAt ? Math.max(0, Math.round((new Date(breakdown.closedAt).getTime() - new Date(breakdown.startedAt).getTime()) / 60000)) : null; return <div className="card"><h2>Arıza Bilgileri</h2><p>{breakdown.description}</p><div className="detail-grid"><div><b>Bildiren</b><br />{breakdown.createdByName}</div><div><b>Teknisyen</b><br />{breakdown.assignedTechnicianName || 'Atanmadı'}</div><div><b>Motor Saati</b><br />{breakdown.motorHours ?? '—'}</div><div><b>Duruş Başlangıcı</b><br />{breakdown.downtimeStartedAt ? new Date(breakdown.downtimeStartedAt).toLocaleString('tr-TR') : '—'}</div><div><b>İşe Başlama</b><br />{breakdown.startedAt ? new Date(breakdown.startedAt).toLocaleString('tr-TR') : '—'}</div><div><b>Çözüm Süresi</b><br />{duration === null ? '—' : `${Math.floor(duration / 60)}s ${duration % 60}dk`}</div></div></div>; }

