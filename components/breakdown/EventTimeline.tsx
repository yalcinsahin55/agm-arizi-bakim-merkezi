export default function EventTimeline({events}:{events:any[]}){return <div className="card" style={{marginTop:16}}><h2>Olay Geçmişi</h2><div className="timeline">{events.map((e:any)=><div className="event" key={String(e._id)}><b>{e.type}</b><div>{e.actorName} · {new Date(e.createdAt).toLocaleString('tr-TR')}</div>{e.note&&<div className="muted">{e.note}</div>}</div>)}</div></div>}
