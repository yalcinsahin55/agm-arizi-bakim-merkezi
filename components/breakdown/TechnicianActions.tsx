'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function TechnicianActions({breakdown}:{breakdown:any}){
 const r=useRouter(); const [report,setReport]=useState(''),[rootCause,setRootCause]=useState(''),[correctiveAction,setCorrectiveAction]=useState(''),[parts,setParts]=useState(''),[materials,setMaterials]=useState(''),[busy,setBusy]=useState(false);
 async function act(action:string,extra:any={}){setBusy(true);const x=await fetch(`/api/breakdowns/${breakdown._id}/action`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,...extra})});setBusy(false);if(x.ok)r.refresh();else alert((await x.json()).error)}
 return <div className="form">{!breakdown.seenAt&&<button disabled={busy} className="btn primary" onClick={()=>act('seen')}>Bildirimi Gördüm</button>}{['atandi','revizyon'].includes(breakdown.status)&&!breakdown.acknowledgedAt&&<button disabled={busy||!breakdown.seenAt} className="btn primary" onClick={()=>act('accept')}>İşi Kabul Et</button>}{['atandi','revizyon'].includes(breakdown.status)&&breakdown.acknowledgedAt&&<button disabled={busy} className="btn primary" onClick={()=>act('start')}>Müdahaleye Başla</button>}{breakdown.status==='devam_ediyor'&&<><label>Teknik Rapor<textarea value={report} onChange={e=>setReport(e.target.value)} placeholder="Tespit, yapılan işlemler ve sonuç..."/></label><label>Kök Neden<textarea value={rootCause} onChange={e=>setRootCause(e.target.value)} placeholder="Arızanın temel nedeni..."/></label><label>Düzeltici Faaliyet<textarea value={correctiveAction} onChange={e=>setCorrectiveAction(e.target.value)} placeholder="Yapılan kalıcı çözüm..."/></label><label>Değiştirilen Parçalar<textarea value={parts} onChange={e=>setParts(e.target.value)} placeholder="Her satıra bir parça"/></label><label>Kullanılan Malzemeler<textarea value={materials} onChange={e=>setMaterials(e.target.value)} placeholder="Her satıra bir malzeme"/></label><button disabled={busy||report.trim().length<10} className="btn primary" onClick={()=>act('submit',{report:report.trim(),rootCause:rootCause.trim(),correctiveAction:correctiveAction.trim(),parts:parts.split('\n').map(x=>x.trim()).filter(Boolean),materials:materials.split('\n').map(x=>x.trim()).filter(Boolean)})}>Raporu Yöneticiye Gönder</button></>}</div>
}
