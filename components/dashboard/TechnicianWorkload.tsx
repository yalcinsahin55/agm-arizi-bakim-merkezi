import Link from 'next/link';
export default function TechnicianWorkload({items}:{items:any[]}){return <section className="card" style={{marginTop:18}}><div className="row" style={{justifyContent:'space-between'}}><div><h2>Teknisyen İş Yükü</h2><p className="muted">Aktif işler ve devam eden müdahaleler</p></div><Link className="btn" href="/yonetim/kullanicilar">Kullanıcılar</Link></div><div className="workload">{items.map((t:any)=><div className="workload-row" key={t.name}><span>{t.name}</span><div><div className="workload-bar"><i style={{width:`${Math.min(100,t.count*20)}%`}}/></div><small>{t.started} devam ediyor</small></div><b>{t.count}</b></div>)}</div></section>}
