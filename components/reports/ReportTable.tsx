export default function ReportTable({ rows }: {
    rows: any[];
}) { const fmt = (a: Date, b: Date) => { const m = Math.max(0, Math.round((b.getTime() - a.getTime()) / 60000)); return `${Math.floor(m / 60)}s ${m % 60}dk`; }; return <div className="card" style={{ marginTop: 16, overflowX: 'auto' }}><h2>Arıza Detayları ({rows.length})</h2><table className="table"><thead><tr><th>Kod</th><th>Tarih</th><th>Motor</th><th>Kategori</th><th>Teknisyen</th><th>Durum</th><th>MTTR</th></tr></thead><tbody>{rows.slice(0, 500).map((x: any) => <tr key={x._id}><td>{x.code}</td><td>{new Date(x.createdAt).toLocaleString('tr-TR')}</td><td>{x.motorName}</td><td>{x.categoryName}</td><td>{x.assignedTechnicianName || '—'}</td><td>{x.status}</td><td>{x.startedAt && x.closedAt ? fmt(new Date(x.startedAt), new Date(x.closedAt)) : '—'}</td></tr>)}</tbody></table></div>; }

