export const sw=`self.addEventListener('push',e=>{const d=e.data?.json?.()||{};e.waitUntil(self.registration.showNotification(d.title||'AGM Arıza', {body:d.body||'',data:d.data||{}}))});self.addEventListener('notificationclick',e=>{e.notification.close();const href=e.notification.data?.href||'/';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs){if('focus' in c){c.navigate(href);return c.focus()}}return clients.openWindow(href)}))});`;
